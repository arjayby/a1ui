# Set to the public HTTPS origin before building for deployment.
# Used by install commands, copied prompts, registry metadata, and agent guides.
# No trailing path, query string, or credentials.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
